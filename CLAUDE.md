# CLAUDE.md

**This repository is retired and serves nothing. Read `README.md` before doing any work here.**

If you have been asked to change the emp54 landing page, the marketing copy, or anything a
visitor to `emp54.com` sees, **you are in the wrong repository**. That page is served by the
Django app in `mark-artim/vue-basic` (locally `projects/vue-basic`) — see
`django-backend/emp54_django/urls.py` (`home_view`) and `core/views.py` (`landing_page`), and
read that repo's `CLAUDE.md` first.

The files here still render, which is what makes the mistake easy to make. Editing them has
no effect on anything live.
